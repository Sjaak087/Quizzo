{
  "rules": {
    "users":   { "$u":    { ".read": true, ".write": "!data.exists()" } },
    "emails":  { "$e":    { ".read": true, ".write": "!data.exists()" } },
    "quizzes": { "$u":    { ".read": true, ".write": true } },
    "games":   { "$code": { ".read": true, ".write": true } }
  }
}
